[project]
name = "pathfinder"
version = "0.1.0"
description = "PathFinder Slice 2 — personalized learning paths"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]
