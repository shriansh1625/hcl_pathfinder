[project]
name = "pathfinder"
version = "0.1.0"
description = "PathFinder Slice 1 — career gap intelligence"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]
