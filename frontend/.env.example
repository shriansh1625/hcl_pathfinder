# PathFinder local frontend
# Copy to frontend/.env.local before npm run dev or npm run build.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: dev server port (default 3000 when unset)
# PORT=3000
